---
const { title = "Yarmouth Chamber Music Festival", description = "Yarmouth Chamber Music Festival" } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600&family=Inter:wght@400;600&display=swap"
      rel="stylesheet"
    />
    <style>
      :root {
        color-scheme: light;
        font-family: "Inter", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
        line-height: 1.6;
        --accent: #14365d;
        --accent-light: #f1f6fb;
        --warm: #f8ede3;
      }

      body {
        margin: 0;
        background: #fff;
        color: #1f2933;
      }

      header {
        background: var(--accent);
        color: #fff;
        padding: 1rem 0;
      }

      .container {
        width: min(1100px, 92%);
        margin: 0 auto;
      }

      header nav {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 1.5rem;
      }

      header a {
        color: inherit;
        text-decoration: none;
        font-weight: 600;
        letter-spacing: 0.02em;
      }

      header .nav-links {
        display: flex;
        gap: 1.25rem;
      }

      main {
        padding-bottom: 4rem;
      }

      footer {
        background: var(--accent-light);
        padding: 2rem 0;
        font-size: 0.925rem;
      }

      footer a {
        color: inherit;
      }

      @media (max-width: 720px) {
        header nav {
          flex-direction: column;
          align-items: flex-start;
        }

        header .nav-links {
          flex-wrap: wrap;
        }
      }
    </style>
  </head>
  <body>
    <header>
      <div class="container">
        <nav>
          <a href="/">Yarmouth Chamber Music Festival</a>
          <div class="nav-links">
            <a href="/#schedule">Schedule</a>
            <a href="/#about">About</a>
            <a href="/#support">Support</a>
          </div>
        </nav>
      </div>
    </header>
    <main>
      <slot />
    </main>
    <footer>
      <div class="container">
        <p><strong>Yarmouth Chamber Music Festival</strong> • October 3–12, 2024 • Yarmouth, Nova Scotia</p>
        <p>Modeled after the Newburyport Chamber Music Festival. Join the newsletter for updates and ticket releases.</p>
      </div>
    </footer>
  </body>
</html>
